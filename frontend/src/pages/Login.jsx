/**
 * Login.jsx — Placeholder. Formulaire de connexion à implémenter ensuite.
 */
export default function Login() {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <h1 className="text-2xl font-semibold">Login — à implémenter</h1>
        </div>
    );
}
